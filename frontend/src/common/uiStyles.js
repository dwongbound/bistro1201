/**
 * Shared UI tokens for panel-style surfaces so page sections keep the same shape language.
 */
export const PANEL_BORDER_RADIUS = '10px';

export const panelCardSx = {
  borderRadius: PANEL_BORDER_RADIUS,
};
